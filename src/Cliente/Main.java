package Cliente;

import java.io.IOException;

import Servidor.ServerGUI;


public class Main {

    public static void main(String[] args) {
        /*ServerGUI serverGUI = new ServerGUI();

        // Mostrar la ventana ServerGUI
        serverGUI.setVisible(true);

        // Esperar a que el botón "Conectar" sea presionado
        serverGUI.btnConectar().addActionListener(e -> {
            // Crear una instancia de WindowStart
            WindowStart windowStart = new WindowStart();

            // Mostrar la ventana WindowStart
            windowStart.setVisible(true);
        });*/
    }
}